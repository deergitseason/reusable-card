here is my current repo. i "fixed" the problem by putting a vertical stack card by default, which gives a more robust card editing system.

here's what i'd like to do:

1) remove the code editor and change card buttons. Instead in that spot, give a note that says "Note: Leave "Title" field below empty (this is in reference to the vertical stack card having a "title" field)

2) have the watermark give an alt text when hovered over, that gives the parent card hash.